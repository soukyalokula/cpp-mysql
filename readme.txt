create.txt
A text file consisting of the queries to create the tables.

load.cc
This file reads the queries from create.txt, connects to MYSQL database and creates the tables.

query.cc
This file takes user input and retrieves the data from MYSQL database.